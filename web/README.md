# Swipe web (Phase 3)

The React + TypeScript frontend is built in Phase 3 (see docs/06-frontend.md and
docs/08-roadmap.md). Scaffolded then with:

    npm create vite@latest . -- --template react-ts

For now the API serves interactive docs at http://localhost:8000/docs — enough
to exercise every Phase 0 endpoint.
